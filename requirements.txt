# Bibliothèque pour l'analyse de HTML et XML (parsing web)
beautifulsoup4>=4.11.1

# Framework de test unitaire (recommandé pour les tests)
pytest>=7.0.0

# Bibliothèque pour les requêtes WHOIS (renseignements sur les domaines)
python-whois>=0.7.3

# Bibliothèque pour les requêtes HTTP (communication réseau)
requests>=2.28.0

# Bibliothèque pour l'analyse de HTML et XML (parsing web)
beautifulsoup4>=4.12

# Liaison Python pour Nmap (scanner réseau, optionnel)
python-nmap>=0.7.1

# Support YAML pour les fichiers de configuration
PyYAML>=6.0

# Résolution DNS (résolution des noms de domaine)
dnspython>=2.3.0

# Génération de PDF pour les rapports
reportlab>=4.0.0

# Interface graphique (optionnel)
PyQt5>=5.15.0

# Framework de test unitaire
pytest>=7.2

# Couverture de code pour les tests
pytest-cov>=4.0

# Gestion des variables d'environnement (optionnel mais pratique)
python-dotenv>=1.0

# Barre de progression pour les tâches longues (optionnel mais utile)
tqdm>=4.65


dnspython
reportlab
# pysqlite removed: using stdlib JSON file persistence instead of SQLite